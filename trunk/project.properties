#default target in case the module does not override it
jar.filename=${module.name}.jar
jar.file=${dist.dir}/${jar.filename}
project.release.version=0.1
project.website=http://code.google.com/p/mismo-bindings/
project.full.name=${ant.project.name}-${project.release.version}

#package under which binding classes will be generated 
binding.pkg=com.sysdelphia.bindings.mismo
binding.xml=com/sysdelphia/bindings/mismo/binding.xml
module.target=jar
